import 'package:flutter/material.dart';
import 'food.dart';

void main() {
  runApp(MaterialApp(
    home: FoodPage(), // Insert the real Home page; currently the first page shown is the food page
  ));
}

